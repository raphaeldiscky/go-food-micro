// Package components provides constants for the tracing components.
package components

// constants for the tracing components.
const (
	CommandHandler = "command_handler"
	RequestHandler = "request_handler"
	QueryHandler   = "query_handler"
	EventHandler   = "event_handler"
)
